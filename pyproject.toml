[project]
name = "homework-mcp"
version = "0.1.0"
description = "Custom MCP Math and Database Tool Server"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.4.4",
    "mcp>=1.28.1",
    "openai>=2.46.0",
    "pydantic>=2.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
